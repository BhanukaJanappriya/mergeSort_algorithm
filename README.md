# mergeSort_algorithm
Traditional Merge Sort divides the array into two parts. Implement a variation of Merge Sort that  recursively divides the array into three parts instead of two, sort each part recursively using this 3-way  Merge Sort and merge the 3 sorted subarrays into one sorted array.  
